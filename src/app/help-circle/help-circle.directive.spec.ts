import { HelpCircleDirective } from './help-circle.directive';

describe('HelpCircleDirective', () => {
  it('should create an instance', () => {
    const directive = new HelpCircleDirective();
    expect(directive).toBeTruthy();
  });
});
